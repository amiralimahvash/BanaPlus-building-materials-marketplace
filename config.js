// config.js — تنظیمات قابل ویرایش پروژه
// این مقادیر را قبل از اجرای واقعی سایت با اطلاعات خودتان جایگزین کنید.

module.exports = {
  // اطلاعات کارت برای پرداخت کارت‌به‌کارت (Escrow دستی)
  payment: {
    cardNumber: '6037-XXXX-XXXX-XXXX',   // شماره کارت خودتان را اینجا جایگزین کنید
    cardHolder: 'نام و نام خانوادگی صاحب کارت',
    bankName: 'نام بانک',
  },

  // رمز ورود پنل ادمین برای تأیید سفارش‌ها
  // حتماً قبل از انتشار واقعی سایت این رمز را عوض کنید.
  adminPassword: 'admin1234',

  // شبیه‌سازی نمای ساختمان با هوش مصنوعی (اعمال سنگ روی عکس نما)
  // این قابلیت از OpenAI Images API (مدل gpt-image-1) استفاده می‌کند.
  // یک کلید API از https://platform.openai.com دریافت کنید و اینجا جایگزین کنید.
  // توجه: این سرویس پولی است و هر تولید تصویر هزینه دارد.
  aiMockup: {
    apiKey: 'YOUR_OPENAI_API_KEY_HERE',
    model: 'gpt-image-1',
  },

  // پیامک تایید هویت (OTP) هنگام ورود و ثبت‌نام — با استفاده از سرویس sms.ir (متد Verify)
  // برای محیط تست (Sandbox): از کلید API نوع Sandbox استفاده کنید (بدون هزینه و بدون ارسال پیامک واقعی).
  //   مسیر ساخت کلید: برنامه‌نویسان ← لیست کلیدهای API ← ایجاد کلید جدید (نوع: Sandbox)
  //   در Sandbox فقط قالب پیش‌فرض با شناسه 123456 فعال است.
  // برای محیط اصلی (Production): کلید API واقعی و شناسه قالب Verify ساخته‌شده در پنل sms.ir را جایگزین کنید.
  sms: {
    apiUrl: 'https://api.sms.ir/v1/send/verify',
    apiKey: 'YOUR_SANDBOX_API_KEY',   // کلید Sandbox یا Production خودتان را جایگزین کنید
    templateId: 123456,               // شناسه قالب Verify (پیش‌فرض Sandbox: 123456 — متن: «کد تایید شما: #CODE#»)
    codeLength: 5,                    // طول کد تایید عددی
    codeExpirySeconds: 120,           // مدت اعتبار کد (ثانیه)
    resendDelaySeconds: 60,           // حداقل فاصله بین دو درخواست ارسال کد برای یک شماره
    maxAttempts: 5,                   // حداکثر تعداد تلاش نادرست قبل از باطل شدن کد
  },
};
